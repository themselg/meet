# Configuracion del kiosko. Editar en /etc/meeting-room/kiosk.env (persiste entre updates).

# VM_MODE=0: hardware real con GPU (predeterminado).
# VM_MODE=1: render por software, para VMs sin aceleracion 3D.
VM_MODE=0

# URL del backend y de la pantalla del kiosko
SERVER_URL=http://localhost
KIOSK_URL=http://localhost/kiosk

# Binario de chromium (EPEL lo instala como chromium-browser)
CHROMIUM_BIN=/usr/bin/chromium-browser

# Flags extra para chromium, separados por espacio (opcional)
#CHROMIUM_EXTRA_FLAGS=

# Soporte remoto VNC (wayvnc) dentro de la sesion del kiosko.
# Escucha SOLO en localhost; conectarse via tunel SSH:
#   ssh -L 5900:localhost:5900 usuario@appliance   y VNC a localhost:5900
# KIOSK_VNC=0 lo desactiva.
KIOSK_VNC=1
#KIOSK_VNC_PORT=5900
